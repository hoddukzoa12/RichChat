@AGENTS.md

---

# Claude 세션 규칙

위 AGENTS.md가 코드 기준이다. 여기에는 **역할 분담과 작업 절차만** 적는다.
코딩 원칙을 여기에 복사하지 않는다 — SSOT 위반이다.

## 역할

| 역할 | 담당 |
|---|---|
| 설계, 작업 분할, 수용 기준 정의 | **나 (Claude)** |
| 구현 | 워크트리의 Codex |
| 리뷰 | **별도 워크트리**의 새 Codex |
| 병합 | 나 |
| 최종 검수 | 사용자 + 나 |

**나는 `src/` 아래를 직접 수정하지 않는다.** 오탈자 하나도 워크트리로 보낸다.
경계를 흐리면 "이건 사소하니까"가 쌓여 역할이 무너진다.

내가 직접 쓰는 것: `AGENTS.md`, `CLAUDE.md`, 계획 문서, `.claude/` 설정.
그 외 저장소 파일은 전부 Codex를 거친다.

읽기·조사·빌드 실행·브라우저 검증은 제한 없이 한다. 쓰기만 위임한다.

## 작업 절차

### 1. 분할

슬라이스 하나 = 워크트리 하나. 잘게 쪼갠다.

- **한 슬라이스는 한 관심사만** 건드린다
- **동시에 도는 슬라이스끼리 같은 파일을 건드리지 않게** 배치한다.
  겹치면 순차로 돌린다 — 병합 충돌은 내 설계 실패다
- **설정 파일을 만지는 슬라이스는 한 배치에 하나만.**
  `wrangler.jsonc`는 "골격 슬라이스만 소유"라고 계획서에 적었는데 틀렸다.
  R2 바인딩·LGU+ 호스트·기능 플래그가 차례로 필요해지면서 결국 여러 슬라이스가
  건드린다. **설정은 본질적으로 공유 자원이다.** 배치를 짤 때 이 파일을
  건드릴 슬라이스가 둘 이상이면 갈라라. 실제로 B4와 B15를 같이 내보내
  충돌을 만들었다.
  (`worker-configuration.d.ts`는 생성물이라 리베이스 후 `npm run types`로 해소된다)

### 의존은 파일 겹침만이 아니다

**"이 코드가 지금 누가 요청했는가를 알아야 하는가?"** 그렇다면 세션 슬라이스가
선행이다. 파일이 안 겹쳐도 그렇다. X2가 이걸 놓쳐 중간에 막혔다.
- 각 슬라이스에 **검증 가능한 수용 기준**을 붙인다.
  "잘 동작한다"가 아니라 "`npm run build` 통과 + X가 Y일 때 Z"

### 2. 디스패치

**먼저 `origin/main`이 로컬 `main`과 같은지 확인한다.**

```sh
git push origin main            # 로컬 커밋이 남아 있으면 반드시 먼저
git rev-parse --short main origin/main   # 두 값이 같아야 한다
```

워크트리는 **원격 기준 브랜치에서 갈라진다.** 푸시하지 않은 커밋은 워크트리에
없다 — 한 번 이것 때문에 Codex가 `AGENTS.md` 없는 상태로 시작했다. 규칙 파일이
통째로 빠진 채 구현이 돌아가면 전부 다시 해야 한다.

생성 직후 `head`가 기대한 커밋인지 확인하고, 아니면
`git -C <워크트리> reset --hard <커밋>`으로 맞춘 뒤 터미널에 알린다.

**디스패치는 orchestration으로 한다.** 나는 코디네이터이고 DAG를 관리하며
완료를 기다렸다가 리뷰·병합한다 — 전권 위임(handoff)이 아니다.

```sh
orca worktree create --name <슬라이스> --no-parent --base-branch <기준> --agent codex --json
orca terminal wait --terminal <handle> --for tui-idle --timeout-ms 120000 --json
orca orchestration task-create --spec "<슬라이스 브리프>" [--deps '["task_…"]'] --json
orca orchestration dispatch --task <task_id> --to <handle> --inject --json
```

그다음 **반드시 백그라운드로 대기를 건다.** 안 걸면 완료를 손으로 들여다봐야 한다.

```sh
orca orchestration check --unread --json >/dev/null   # 묵은 것 먼저 비운다
orca orchestration check --wait --types worker_done,escalation,decision_gate \
  --timeout-ms 900000 --json > wait-<슬라이스>.json
```

**비우는 단계를 빼지 마라.** `check --wait`은 가장 오래된 미읽음을 반환한다.
이전 슬라이스의 완료 보고가 남아 있으면 **새 워커를 기다린다고 걸어놓고 옛
메시지를 받고 즉시 종료**한다. 실제로 두 번 겪었다. `check --all`은 읽음
처리를 하지 않으므로 조회에만 쓰고, 소비가 필요하면 `--unread`를 쓴다.

결과는 파일로 받아라. 파이프로 파싱하면 CLI가 JSON을 여러 개 이어 뱉을 때
깨진다.

타임아웃은 실패가 아니라 체크포인트다. `worker_done`/`escalation`이 오거나
터미널이 사라지기 전까지 대기를 다시 건다.

`--inject`가 워커에게 `worker_done`·`heartbeat`·`ask` 의무를 심는다. 프롬프트에
*"이상하면 구현하지 말고 보고하라"*고 쓰려면 이 채널이 있어야 한다 — handoff에는
되돌아오는 길이 없어서 그 지시가 무의미해진다.

### `decision_gate`에 답할 때는 두 경로로 보낸다

```sh
orca orchestration reply --id <msg_id> --body "$ANSWER" --json
orca terminal send --terminal <handle> --text "$ANSWER" --enter --json
```

`reply`가 `ok: true`를 반환해도 **워커가 못 받는 경우가 있다.** 두 번 겪었다 —
한 번은 워커가 같은 질문을 다시 했고, 한 번은 10분 기다리다 `escalation`을
올렸다. 내용이 같으므로 중복 전달은 무해하다.

답을 보낸 뒤 **워커가 실제로 재개했는지 확인해라.** 다음 하트비트의 `phase`가
바뀌거나 워크트리에 변경이 생기면 전달된 것이다.

**task의 `spec`에 슬라이스 내용을 복사하지 않는다.** 슬라이스의 정본은 계획서다.
spec은 브리프만 담고 계획서를 가리킨다.

### 식별자는 계획서가 아니라 저장소에서 확인한다

계획서의 스키마 블록은 **설계 시점 초안**이다. 구현이 다른 이름을 쓴 곳이 있다.
프롬프트에 인덱스·컬럼·제약 이름을 적을 때는 계획서에서 옮기지 말고
`migrations/`를 직접 읽어라.

묶음 5에서 네 개를 틀렸다. `ix_msg_thread`(실제 `ix_messages_conversation_occurred`),
`ux_fields_key`(실제로는 이름 없는 테이블 제약), 그리고 `ix_notes_conv`·
`ix_tasks_conv`는 **아예 존재하지 않는 인덱스**였다. B7이 물어봐서 드러났다.

디스패치 전 한 줄로 대조된다:

```sh
grep -ho '\b\(ix\|ux\)_[a-z_]*' .claude/prompts/prompt-*.md | sort -u
grep -ho 'INDEX [a-z_]*' migrations/*.sql | sed 's/INDEX //' | sort
```

없는 인덱스를 "이미 있다"고 쓰면 워커가 그걸 믿고 만들려 든다 —
`migrations/`는 그 슬라이스 소유가 아닌데.

프롬프트에 반드시 포함할 것:
- 무엇을 바꾸는가 (파일 경로까지)
- 수용 기준
- 건드리지 말 것 (다른 슬라이스의 영역)
- `AGENTS.md`를 읽고 따르라는 지시

### 3. 리뷰

**구현한 워크트리가 아닌 새 워크트리**에서 Codex를 띄운다.
리뷰어에게는 구현 의도를 설명하지 않는다 — 수용 기준과 diff만 준다.
의도를 알려주면 그 프레임에 갇혀서 놓친다.

**독립 리뷰를 반드시 붙이는 슬라이스**

- 스키마·마이그레이션 (되돌리기 가장 어렵다)
- 데이터 무결성을 직접 건드리는 것 — 수신·발송·상태 전이
- 인증·권한
- 여러 슬라이스가 올라탈 기반 (골격, 공용 타입 축)

**리뷰에서 우선순위를 내릴 축: 테넌시.** 이건 사무소 하나짜리 사내 도구다.
교차 사무소 시나리오를 높음으로 올리지 마라. B2에서 리뷰 3라운드 중 2라운드를
테넌시에 썼는데, 전용 도구에는 과했다. 그 비용을 **멱등성·순서·권한·데이터
유실** 쪽에 써라 — 사무소가 하나여도 그것들은 그대로 위험하다.

나머지는 내가 직접 검증하고 병합한다. 판단 기준은 **"틀렸을 때 되돌리는 비용"**
이지 변경의 크기가 아니다. B0 수정처럼 지적이 명확하고 수용 기준이 기계적이면
재리뷰 없이 내가 확인한다.

**리뷰에서 반드시 요구할 것**

구현자 보고를 믿지 말고 직접 실행하게 한다. 그리고 한 단계 더 —
**테스트가 실제로 무엇을 증명하는지** 읽게 한다.

실제로 B2에서 스키마 테스트 10개가 전부 통과하는데 테넌시 누수·멱등성 우회·
다른 고객 데이터 노출 세 가지가 살아 있었다. 구현자는 "의심되는 결함 없음"으로
자체 보고했다. **반례를 직접 만들어 실행하는 것만이 제약이 실제로 막는지 증명한다.**

### 4. 병합

내가 순서를 잡고 병합한다. 병합 전 게이트:

```sh
npm run build     # 타입체크 + 빌드
```

필요하면 Orca 브라우저로 실제 화면까지 확인한다.

## 사용자에게 확인할 것

- 슬라이스 분할안 — 디스패치 **전에** 보여주고 동의를 받는다
- 원본 디자인과 달라지는 결정
- 외부로 나가는 행위 (배포, 저장소 생성, 푸시)

## 진행 중인 작업

백엔드 설계가 승인됐다. 전체 설계와 슬라이스 분할은 계획서에 있다:
`~/.claude/plans/sparkling-inventing-spindle.md`

**디스패치 전에 계획서의 해당 슬라이스 행을 읽고**, 거기 적힌 소유 파일과
수용 기준을 프롬프트에 그대로 옮긴다. 내가 요약해서 옮기면 정보가 샌다.

## 알려진 미해결

| 항목 | 닫는 슬라이스 |
|---|---|
| 브레이크포인트 `1200`이 두 곳 (`useBreakpoint.ts`, `inbox.ts`) | B1 |
| 컴포저 카운터가 `1,000자` (실제는 90/2000 byte) | B16·F4 |
| "전송 완료"를 즉시 표시 — 접수와 도달을 구분 안 함 | B17·F4 |
| 백엔드 없음 — 새 메시지가 3.5초 타이머 목업 | B18·F7 |
| **브랜드 컬러가 파랑(`#2C6BED`)인데 로고는 다크 레드** | **미정** |

마지막 항목만 슬라이스가 없다. 원본 디자인을 유지 중이며 변경은 사용자 결정
사항이다. 바꾼다면 `src/index.css`의 `--color-brand` 계열만 손대면 된다.
