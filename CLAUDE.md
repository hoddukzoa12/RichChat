@AGENTS.md

---

# Claude 세션 규칙

위 AGENTS.md가 코드 기준이다. 여기에는 **역할 분담과 작업 절차만** 적는다.
코딩 원칙을 여기에 복사하지 않는다 — SSOT 위반이다.

## 역할

| 역할 | 담당 |
|---|---|
| 설계, 작업 분할, 수용 기준 정의 | **나 (Claude)** |
| 구현 | 워크트리의 Codex |
| 리뷰 | **별도 워크트리**의 새 Codex |
| 병합 | 나 |
| 최종 검수 | 사용자 + 나 |

**나는 `src/` 아래를 직접 수정하지 않는다.** 오탈자 하나도 워크트리로 보낸다.
경계를 흐리면 "이건 사소하니까"가 쌓여 역할이 무너진다.

내가 직접 쓰는 것: `AGENTS.md`, `CLAUDE.md`, 계획 문서, `.claude/` 설정.
그 외 저장소 파일은 전부 Codex를 거친다.

읽기·조사·빌드 실행·브라우저 검증은 제한 없이 한다. 쓰기만 위임한다.

## 작업 절차

### 1. 분할

슬라이스 하나 = 워크트리 하나. 잘게 쪼갠다.

- **한 슬라이스는 한 관심사만** 건드린다
- **동시에 도는 슬라이스끼리 같은 파일을 건드리지 않게** 배치한다.
  겹치면 순차로 돌린다 — 병합 충돌은 내 설계 실패다
- **설정 파일을 만지는 슬라이스는 한 배치에 하나만.**
  `wrangler.jsonc`는 "골격 슬라이스만 소유"라고 계획서에 적었는데 틀렸다.
  R2 바인딩·LGU+ 호스트·기능 플래그가 차례로 필요해지면서 결국 여러 슬라이스가
  건드린다. **설정은 본질적으로 공유 자원이다.** 배치를 짤 때 이 파일을
  건드릴 슬라이스가 둘 이상이면 갈라라. 실제로 B4와 B15를 같이 내보내
  충돌을 만들었다.
  (`worker-configuration.d.ts`는 생성물이라 리베이스 후 `npm run types`로 해소된다)

### "네가 판단해라"는 뒤를 이을 슬라이스를 만든 뒤에 써라

프롬프트에서 판단을 위임하면 워커는 **자기 범위를 좁히는 쪽으로** 판단한다.
그게 옳은 행동이다. 문제는 좁힌 나머지를 내가 안 받으면 아무도 안 한다는 것이다.

B18에 "첨부 바이너리 다운로드를 이 슬라이스에서 할지 뺄지 네가 판단해라"라고
썼다. B18은 뺐고, 그 뒤를 이을 슬라이스를 내가 안 만들었다. 결과는 첨부
메타데이터만 남고 바이트는 영원히 안 받는 상태였다 — 독립 리뷰가 잡았다.

위임하려면 **두 갈래 모두에 소유자가 있어야 한다.** 없으면 위임하지 말고
범위를 못 박아라.

### 소유는 파일이 아니라 **HTTP 경로** 단위로 정해라

라우터는 파일이 아니라 경로를 매핑한다. 두 슬라이스가 서로 다른 파일을
소유해도 **같은 경로를 등록하면 먼저 등록된 쪽만 살고 나머지는 죽은 코드**가 된다.

계획서가 B4를 "세션 + `/api/me`"(`routes/me.ts`), B13을 "내 설정"
(`routes/settings-me.ts`)으로 나눴는데 `/api/me/settings`가 어느 쪽인지
불분명했다. B4가 두 PATCH를 다 만들었고, `settings-me.ts`에 구현했으면
**영원히 도달하지 않았다.**

슬라이스를 짤 때 소유 파일 옆에 **등록할 경로를 명시**하고, 배치 안에서
경로가 겹치는지 먼저 본다:

```sh
python3 .claude/scripts/routes.py
```

겹치면 슬라이스를 합치거나 경로를 갈라라. 파일만 갈라서는 해결되지 않는다.

**grep으로 하지 마라.** 처음에 `grep "path: '...'"`로 짰다가 상수로 정의된
경로(`path: TASK_ITEM_PATH`)를 통째로 놓쳤다. 업무 라우트 3개와 OIDC 콜백이
목록에서 빠졌는데 **"중복 없음"이라고 통과했다.** 실제 요청을 보내보고서야
알았다. 잘못된 안심을 주는 도구는 없는 것보다 나쁘다.

스크립트는 같은 파일의 `const X = '...'`를 해석하고, 해석 못 한 항목은
경고로 남긴다. 조용히 빠뜨리지 않는 것이 요점이다.

### 범위를 넘어 만든 코드에는 리뷰 축이 없다

B4는 독립 리뷰를 **통과**했는데, 나중에 B13이 같은 영역을 검증하니 이벤트
미발행·불리언 정규화·직접 시계 호출 세 결함이 나왔다.

리뷰 프롬프트에 그 축을 안 적었기 때문이다. **리뷰는 물어본 것만 답한다.**

병합할 때 `filesModified`가 선언한 소유 범위를 넘었는지 보고, 넘었으면
그 부분에 **리뷰 축을 추가하거나 뒤 슬라이스의 검증 대상으로 넘겨라.**

### 계획서의 슬라이스가 이미 구현돼 있는지 확인해라

계획서는 **설계 시점의 분할**이다. 앞선 슬라이스가 자기 범위를 넘어 이웃까지
만들어 두는 일이 실제로 있다.

B13(내 설정)을 내보냈는데 B4가 이미 `PATCH /api/me`와 `/api/me/settings`를
만들어 놨고 `settings-me.ts`는 빈 스텁이었다. 게다가 `worker/index.ts`에서
`meRoutes`가 먼저 등록돼 **스텁에 구현해도 영원히 도달하지 않는다.**
워커가 물어봐서 드러났다.

디스패치 전에 그 슬라이스가 소유할 경로를 grep 한 번 해라:

```sh
grep -rn "path: '/api/<경로>'" worker/routes/
```

이미 있으면 슬라이스를 **구현이 아니라 검증·보강으로 돌려라.**
없는 일을 시키면 워커가 막히거나, 더 나쁘게는 **도달하지 않는 코드를 만든다.**

**빈 스텁을 그대로 두지 마라.** "여기가 주인"이라고 거짓말하는 파일은
다음 사람을 같은 함정에 빠뜨린다. 소유자가 확정되면 스텁을 지운다.

### 설계를 조언할 때도 코드를 먼저 확인해라

식별자만 저장소에서 확인하면 되는 게 아니다. **"이렇게 하면 된다"는
조언 자체가 코드와 안 맞을 수 있다.**

F5에게 "조립된 리듀서는 같은 액션에 여러 슬라이스가 각자 반응할 수 있다"고
단언했는데, `src/state/inbox.ts`는 `{...aHandlers, ...bHandlers}` 스프레드라
**마지막 핸들러가 이긴다.** 그대로 갔으면 F4의 `select` 처리가 조용히
삼켜졌을 것이다. F5가 물어봐서 드러났다.

일반론으로 답하기 전에 그 파일을 열어라. 한 번 보면 끝나는 일이고,
안 보면 워커가 내 말을 믿고 구현한다.

**대안이 이미 저장소에 있는 경우가 많다.** 이번에도 F4가 `Record<대화id, …>`로
키를 둬서 해결해뒀는데, 내가 그걸 안 보고 조립점을 고치라고 할 뻔했다.

### 상태 트리를 공유하면 파일을 갈라도 병렬이 아니다

프론트 슬라이스를 파일 소유로 갈라 넷을 병렬로 냈는데, **하나의 리듀서
상태 트리를 공유**해서 모양을 바꾸는 슬라이스의 브랜치가 단독으로
컴파일되지 않았다. F3이 물어봐서 드러났다.

"완료 판정은 `npm run check` 통과"를 지키려면 **모양을 바꾸는 슬라이스가
먼저 병합되고 나머지가 리베이스**해야 한다. 파일 소유만 보고 병렬로 내면
안 된다.

배치를 짤 때 물어라 — **이 슬라이스들이 같은 상태 타입을 읽는가?**
읽으면 그 타입을 바꾸는 슬라이스가 선행이다.

먼저 가는 슬라이스에는 "깨지는 남의 파일을 **컴파일만 되게** 최소로 손보고
동작은 옮기지 마라"고 지시한다. 어차피 소유자가 통째로 다시 쓰므로
호환 코드를 정성껏 쓰면 그게 버려지고 충돌만 남는다.

### 타입도 겹친다 — 파일만 보지 마라

파일이 안 겹쳐도 **같은 엔티티의 타입을 두 슬라이스가 각자 정의**할 수 있다.

묶음 5에서 두 번 났다. B7(상세)이 응답에 업무·메모를 포함하면서
`ConversationTask`·`ConversationNote`를 정의했는데, 같은 배치의 B12(업무)와
B11(메모)이 `Task`·`Note`를 정의했다. 업무는 필드까지 동일했고, 메모는
B7 쪽에만 `authorTitle`이 있었다.

원인은 내 프롬프트다. "상세에 업무·메모를 포함해 내려라"라고 쓰면서
**그 타입을 누가 소유하는지 적지 않았다.**

집합 응답을 내리는 슬라이스에는 이렇게 적어라:

> `X`는 B__ 소유다. `shared/wire/x.ts`에 정의될 것이니 **직접 정의하지 말고
> import 해라.** 아직 스텁이면 필요한 모양을 `ask`로 알려라.

**어느 쪽이 정본인가는 화면이 정한다.** 메모의 `authorTitle`은 프론트엔드가
작성자 이름만 렌더하고 원본 디자인에도 없어서 걷어냈다. 필드가 다를 때
"더 많은 쪽"을 고르지 마라 — 쓰이는 쪽을 골라라.

### 의존은 파일 겹침만이 아니다

**"이 코드가 지금 누가 요청했는가를 알아야 하는가?"** 그렇다면 세션 슬라이스가
선행이다. 파일이 안 겹쳐도 그렇다. X2가 이걸 놓쳐 중간에 막혔다.
- 각 슬라이스에 **검증 가능한 수용 기준**을 붙인다.
  "잘 동작한다"가 아니라 "`npm run build` 통과 + X가 Y일 때 Z"

### 2. 디스패치

**먼저 `origin/main`이 로컬 `main`과 같은지 확인한다.**

```sh
git push origin main            # 로컬 커밋이 남아 있으면 반드시 먼저
git rev-parse --short main origin/main   # 두 값이 같아야 한다
```

워크트리는 **원격 기준 브랜치에서 갈라진다.** 푸시하지 않은 커밋은 워크트리에
없다 — 한 번 이것 때문에 Codex가 `AGENTS.md` 없는 상태로 시작했다. 규칙 파일이
통째로 빠진 채 구현이 돌아가면 전부 다시 해야 한다.

생성 직후 `head`가 기대한 커밋인지 확인하고, 아니면
`git -C <워크트리> reset --hard <커밋>`으로 맞춘 뒤 터미널에 알린다.

**디스패치는 orchestration으로 한다.** 나는 코디네이터이고 DAG를 관리하며
완료를 기다렸다가 리뷰·병합한다 — 전권 위임(handoff)이 아니다.

```sh
orca worktree create --name <슬라이스> --no-parent --base-branch <기준> --agent codex --json
orca terminal wait --terminal <handle> --for tui-idle --timeout-ms 120000 --json
orca orchestration task-create --spec "<슬라이스 브리프>" [--deps '["task_…"]'] --json
orca orchestration dispatch --task <task_id> --to <handle> --inject --json
```

그다음 **반드시 백그라운드로 대기를 건다.** 안 걸면 완료를 손으로 들여다봐야 한다.

```sh
orca orchestration check --unread --json >/dev/null   # 묵은 것 먼저 비운다
orca orchestration check --wait --types worker_done,escalation,decision_gate \
  --timeout-ms 900000 --json > wait-<슬라이스>.json
```

**비우는 단계를 빼지 마라.** `check --wait`은 가장 오래된 미읽음을 반환한다.
이전 슬라이스의 완료 보고가 남아 있으면 **새 워커를 기다린다고 걸어놓고 옛
메시지를 받고 즉시 종료**한다. 실제로 두 번 겪었다. `check --all`은 읽음
처리를 하지 않으므로 조회에만 쓰고, 소비가 필요하면 `--unread`를 쓴다.

결과는 파일로 받아라. 파이프로 파싱하면 CLI가 JSON을 여러 개 이어 뱉을 때
깨진다.

타임아웃은 실패가 아니라 체크포인트다. `worker_done`/`escalation`이 오거나
터미널이 사라지기 전까지 대기를 다시 건다.

`--inject`가 워커에게 `worker_done`·`heartbeat`·`ask` 의무를 심는다. 프롬프트에
*"이상하면 구현하지 말고 보고하라"*고 쓰려면 이 채널이 있어야 한다 — handoff에는
되돌아오는 길이 없어서 그 지시가 무의미해진다.

### 워커를 답 기다리며 멈춰 세우지 마라

꼬리말에 "답이 올 때까지 기다린다"고 썼는데 **그게 워커를 통째로 멈춰 세운다.**
B21이 `worker/index.ts` 한 줄 등록 때문에 600초를 기다리다 에스컬레이션을 올렸다.

지금은 "막힌 지점만 남기고 나머지를 계속하라, 10분 내 답이 없으면
`escalation`을 올려라"로 바꿨다. `ask`가 조용히 만료되는 경우가 실제로 있다.

**조정자 쪽 교훈은 따로다** — 질문이 오면 다른 일을 하다가도 먼저 답해라.
워커 하나가 멈춰 있는 비용이 내 컨텍스트 전환 비용보다 크다.

### `decision_gate`에 답할 때는 두 경로로 보낸다

```sh
orca orchestration reply --id <msg_id> --body "$ANSWER" --json
orca terminal send --terminal <handle> --text "$ANSWER" --enter --json
```

`reply`가 `ok: true`를 반환해도 **워커가 못 받는 경우가 있다.** 두 번 겪었다 —
한 번은 워커가 같은 질문을 다시 했고, 한 번은 10분 기다리다 `escalation`을
올렸다. 내용이 같으므로 중복 전달은 무해하다.

답을 보낸 뒤 **워커가 실제로 재개했는지 확인해라.** 다음 하트비트의 `phase`가
바뀌거나 워크트리에 변경이 생기면 전달된 것이다.

**task의 `spec`에 슬라이스 내용을 복사하지 않는다.** 슬라이스의 정본은 계획서다.
spec은 브리프만 담고 계획서를 가리킨다.

### 식별자는 계획서가 아니라 저장소에서 확인한다

계획서의 스키마 블록은 **설계 시점 초안**이다. 구현이 다른 이름을 쓴 곳이 있다.
프롬프트에 인덱스·컬럼·제약 이름을 적을 때는 계획서에서 옮기지 말고
`migrations/`를 직접 읽어라.

묶음 5에서 네 개를 틀렸다. `ix_msg_thread`(실제 `ix_messages_conversation_occurred`),
`ux_fields_key`(실제로는 이름 없는 테이블 제약), 그리고 `ix_notes_conv`·
`ix_tasks_conv`는 **아예 존재하지 않는 인덱스**였다. B7이 물어봐서 드러났다.

디스패치 전 한 줄로 대조된다:

```sh
grep -ho '\b\(ix\|ux\)_[a-z_]*' .claude/prompts/prompt-*.md | sort -u
grep -ho 'INDEX [a-z_]*' migrations/*.sql | sed 's/INDEX //' | sort
```

없는 인덱스를 "이미 있다"고 쓰면 워커가 그걸 믿고 만들려 든다 —
`migrations/`는 그 슬라이스 소유가 아닌데.

프롬프트에 반드시 포함할 것:
- 무엇을 바꾸는가 (파일 경로까지)
- 수용 기준
- 건드리지 말 것 (다른 슬라이스의 영역)
- `AGENTS.md`를 읽고 따르라는 지시

### 3. 리뷰

**구현한 워크트리가 아닌 새 워크트리**에서 Codex를 띄운다.
리뷰어에게는 구현 의도를 설명하지 않는다 — 수용 기준과 diff만 준다.
의도를 알려주면 그 프레임에 갇혀서 놓친다.

**독립 리뷰를 반드시 붙이는 슬라이스**

- 스키마·마이그레이션 (되돌리기 가장 어렵다)
- 데이터 무결성을 직접 건드리는 것 — 수신·발송·상태 전이
- 인증·권한
- 여러 슬라이스가 올라탈 기반 (골격, 공용 타입 축)

**리뷰에서 우선순위를 내릴 축: 테넌시.** 이건 사무소 하나짜리 사내 도구다.
교차 사무소 시나리오를 높음으로 올리지 마라. B2에서 리뷰 3라운드 중 2라운드를
테넌시에 썼는데, 전용 도구에는 과했다. 그 비용을 **멱등성·순서·권한·데이터
유실** 쪽에 써라 — 사무소가 하나여도 그것들은 그대로 위험하다.

나머지는 내가 직접 검증하고 병합한다. 판단 기준은 **"틀렸을 때 되돌리는 비용"**
이지 변경의 크기가 아니다. B0 수정처럼 지적이 명확하고 수용 기준이 기계적이면
재리뷰 없이 내가 확인한다.

**리뷰에서 반드시 요구할 것**

구현자 보고를 믿지 말고 직접 실행하게 한다. 그리고 한 단계 더 —
**테스트가 실제로 무엇을 증명하는지** 읽게 한다.

실제로 B2에서 스키마 테스트 10개가 전부 통과하는데 테넌시 누수·멱등성 우회·
다른 고객 데이터 노출 세 가지가 살아 있었다. 구현자는 "의심되는 결함 없음"으로
자체 보고했다. **반례를 직접 만들어 실행하는 것만이 제약이 실제로 막는지 증명한다.**

### 4. 병합

내가 순서를 잡고 병합한다. 병합 전 게이트:

```sh
npm run build     # 타입체크 + 빌드
```

필요하면 Orca 브라우저로 실제 화면까지 확인한다.

## 사용자에게 확인할 것

- 슬라이스 분할안 — 디스패치 **전에** 보여주고 동의를 받는다
- 원본 디자인과 달라지는 결정
- 외부로 나가는 행위 (배포, 저장소 생성, 푸시)

## 진행 중인 작업

백엔드 설계가 승인됐다. 전체 설계와 슬라이스 분할은 계획서에 있다:
`~/.claude/plans/sparkling-inventing-spindle.md`

**디스패치 전에 계획서의 해당 슬라이스 행을 읽고**, 거기 적힌 소유 파일과
수용 기준을 프롬프트에 그대로 옮긴다. 내가 요약해서 옮기면 정보가 샌다.

## 알려진 미해결

| 항목 | 상태 |
|---|---|
| 브레이크포인트 `1200`이 두 곳 | ✅ `shared/breakpoints.ts` 한 곳 (B1) |
| 컴포저 카운터가 `1,000자` | ✅ `n / 90 byte · SMS` (B16·F4) |
| "전송 완료"를 즉시 표시 | ✅ `delivery_status` 반영 (B17·F4). 회귀 테스트로 잠금 |
| 새 메시지가 3.5초 타이머 목업 | ✅ 목업 제거 (F3·F6) · 실시간 구독은 F7 |
| **브랜드 컬러가 파랑(`#2C6BED`)인데 로고는 다크 레드** | **미정 — 사용자 결정** |

마지막 항목만 남았다. 원본 디자인을 유지 중이며 바꾼다면 `src/index.css`의
`--color-brand` 계열만 손대면 된다.

## 실제 계정이 없어 검증 못 한 것

배포·QA 연동 시 **반드시 확인해야 한다.** 워커들이 추측하지 않고 남긴 것들이다.

| 항목 | 남긴 슬라이스 |
|---|---|
| 복수 첨부의 `GET /mo/v1/file/{moKey}` 매핑 계약 (지금은 건너뛴다) | X4 |
| 네이버웍스 실제 테넌트의 discovery `issuer`·JWKS | B5 |
| LGU+ QA 발송·리포트 실연동 | B17·B19 |
| 사무소 기본 발신번호(`office_channels`) 실제 값 | B14 |
