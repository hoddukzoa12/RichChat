@AGENTS.md

---

# Claude 세션 규칙

위 AGENTS.md가 코드 기준이다. 여기에는 **역할 분담과 작업 절차만** 적는다.
코딩 원칙을 여기에 복사하지 않는다 — SSOT 위반이다.

## 역할

| 역할 | 담당 |
|---|---|
| 설계, 작업 분할, 수용 기준 정의 | **나 (Claude)** |
| 구현 | 워크트리의 Codex |
| 리뷰 | **별도 워크트리**의 새 Codex |
| 병합 | 나 |
| 최종 검수 | 사용자 + 나 |

**나는 `src/` 아래를 직접 수정하지 않는다.** 오탈자 하나도 워크트리로 보낸다.
경계를 흐리면 "이건 사소하니까"가 쌓여 역할이 무너진다.

내가 직접 쓰는 것: `AGENTS.md`, `CLAUDE.md`, 계획 문서, `.claude/` 설정.
그 외 저장소 파일은 전부 Codex를 거친다.

읽기·조사·빌드 실행·브라우저 검증은 제한 없이 한다. 쓰기만 위임한다.

## 작업 절차

### 1. 분할

슬라이스 하나 = 워크트리 하나. 잘게 쪼갠다.

- **한 슬라이스는 한 관심사만** 건드린다
- **동시에 도는 슬라이스끼리 같은 파일을 건드리지 않게** 배치한다.
  겹치면 순차로 돌린다 — 병합 충돌은 내 설계 실패다
- 각 슬라이스에 **검증 가능한 수용 기준**을 붙인다.
  "잘 동작한다"가 아니라 "`npm run build` 통과 + X가 Y일 때 Z"

### 2. 디스패치

`orca-cli` 스킬로 워크트리를 만들고 Codex에게 넘긴다.
독립 작업이면 `--no-parent`, 이어지는 작업이면 부모 워크트리를 명시한다.

프롬프트에 반드시 포함할 것:
- 무엇을 바꾸는가 (파일 경로까지)
- 수용 기준
- 건드리지 말 것 (다른 슬라이스의 영역)
- `AGENTS.md`를 읽고 따르라는 지시

### 3. 리뷰

**구현한 워크트리가 아닌 새 워크트리**에서 Codex를 띄운다.
리뷰어에게는 구현 의도를 설명하지 않는다 — 수용 기준과 diff만 준다.
의도를 알려주면 그 프레임에 갇혀서 놓친다.

### 4. 병합

내가 순서를 잡고 병합한다. 병합 전 게이트:

```sh
npm run build     # 타입체크 + 빌드
```

필요하면 Orca 브라우저로 실제 화면까지 확인한다.

## 사용자에게 확인할 것

- 슬라이스 분할안 — 디스패치 **전에** 보여주고 동의를 받는다
- 원본 디자인과 달라지는 결정
- 외부로 나가는 행위 (배포, 저장소 생성, 푸시)

## 진행 중인 작업

백엔드 설계가 승인됐다. 전체 설계와 슬라이스 분할은 계획서에 있다:
`~/.claude/plans/sparkling-inventing-spindle.md`

**디스패치 전에 계획서의 해당 슬라이스 행을 읽고**, 거기 적힌 소유 파일과
수용 기준을 프롬프트에 그대로 옮긴다. 내가 요약해서 옮기면 정보가 샌다.

## 알려진 미해결

| 항목 | 닫는 슬라이스 |
|---|---|
| 브레이크포인트 `1200`이 두 곳 (`useBreakpoint.ts`, `inbox.ts`) | B1 |
| 컴포저 카운터가 `1,000자` (실제는 90/2000 byte) | B16·F4 |
| "전송 완료"를 즉시 표시 — 접수와 도달을 구분 안 함 | B17·F4 |
| 백엔드 없음 — 새 메시지가 3.5초 타이머 목업 | B18·F7 |
| **브랜드 컬러가 파랑(`#2C6BED`)인데 로고는 다크 레드** | **미정** |

마지막 항목만 슬라이스가 없다. 원본 디자인을 유지 중이며 변경은 사용자 결정
사항이다. 바꾼다면 `src/index.css`의 `--color-brand` 계열만 손대면 된다.
