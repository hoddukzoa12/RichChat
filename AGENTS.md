# RichChat — 상담 인박스

세무법인 리치의 고객 문의 관리 도구. 카카오톡·문자로 들어오는 고객 문의를
하나의 인박스에서 응대하고, 고객별 세무 정보·업무·문서를 함께 본다.

현재는 목 데이터로 동작하는 프론트엔드다. 백엔드(Cloudflare Workers)는 미구현.

## 스택과 명령

Vite + React 19 + TypeScript + Tailwind v4. 배포 대상은 Cloudflare Workers
(단일 Worker가 정적 자산과 API를 함께 서빙).

```sh
npm run dev      # 개발 서버 :5173
npm run build    # tsc -b && vite build
npm run check    # tsc -b && vitest run  ← 완료 판정은 이것
```

**완료 판정은 `npm run check` 통과다.** `npm run build`만으로는 부족하다 —
워커 코드를 타입체크하지 않고(`tsconfig.app.json`이 `include: ["src"]`),
SQL 제약을 검증하지 못하며, 마이그레이션 적용 여부를 모른다. "유니크 인덱스가
중복 수신을 거부한다"는 타입체커가 절대 못 잡는다.

테스트는 `@cloudflare/vitest-pool-workers`로 workerd 안에서 돌린다 —
`SELF.fetch()`로 라우트를, 실제 D1로 제약을 검사한다.

타입 에러나 실패하는 테스트를 남긴 채 "구현했다"고 보고하지 않는다.

---

## 코딩 원칙

우선순위 순이다. 충돌하면 위가 이긴다.

### 1. SSOT — 한 사실은 한 곳에

가장 중요한 원칙이다. 값·타입·규칙 모두에 적용된다.

**두 군데 이상에 나타나는 값은 이미 버그다.** 한쪽만 고치는 사고가 반드시 난다.
상수로 올리고 양쪽이 그것을 import 한다.

```ts
// ✗ 같은 임계값이 두 파일에 산다
useBreakpoint.ts:  if (width < 1200) return 'tablet'
inbox.ts:          cardOpen: window.innerWidth >= 1200

// ✓ 한 곳에서 정의하고 가져다 쓴다
breakpoints.ts:    export const DESKTOP_MIN = 1200
```

파생 가능한 값은 저장하지 않는다. 목록 미리보기는 마지막 메시지에서 계산하지,
별도 `preview` 필드로 중복 보관하지 않는다.

**DB 읽기 모델의 예외** — 인덱스 가능한 정렬키나 카운터는 파생값이지만 저장해야
한다. 규칙은 이렇다: **포인터는 저장하고 값은 조인한다.**
`last_message_id`는 저장하되 미리보기 *텍스트*는 저장하지 않는다.
그리고 프로젝션은 **원본을 쓰는 같은 트랜잭션에서만** 갱신한다 — 다른 경로에서
건드리면 그게 SSOT 위반이다.

> "하드코딩 금지"를 `const ONE = 1`로 해석하지 말 것. 판단 기준은 두 가지다 —
> **두 곳 이상에 나타나는가**, 또는 **비개발자가 바꿔야 할 값인가**.
> 한 곳에서만 쓰이는 자명한 리터럴은 그대로 둔다.

### 2. 닫힌 집합의 분기는 데이터로

상태·채널처럼 값의 집합이 닫혀 있는 분기는 `if`/`switch` 사슬이 아니라
`Record`로 표현한다. **`src/theme.ts`가 이 저장소의 기준 예시다.**

```ts
// ✓ theme.ts
export const STATUS_BADGE: Record<Status, string> = {
  미처리: 'text-open-fg bg-open-bg',
  처리중: 'text-doing-fg bg-doing-bg',
  완료:   'text-done-fg bg-done-bg',
}
```

`Record<타입, T>`로 쓰면 유니온에 값이 추가될 때 컴파일러가 누락을 잡아준다.
이게 이 패턴을 쓰는 진짜 이유다.

> 다만 **이르게 반환하는 가드절은 중첩이 아니다.** 유효성 검사 2~3단을 억지로
> 룩업 테이블로 바꾸지 말 것. 이 규칙은 "값에 따라 무엇을 고를 것인가"에만 적용된다.

### 3. 모듈 경계

새 코드는 아래 중 하나에 속한다. 어디에도 안 맞으면 **새 축을 만들 근거를 먼저 말한다.**

| 디렉터리 | 책임 | 넣지 말 것 |
|---|---|---|
| `src/state/` | 상태 모양, 리듀서, 액션 | JSX, 스타일 |
| `src/state/selectors.ts` | 상태에서 파생되는 계산 | 상태 변경 |
| `src/data/` | 시드·상수 데이터 | 로직 |
| `src/hooks/` | 재사용 React 훅 | 도메인 규칙 |
| `src/components/` | 표현 | 비즈니스 규칙, 파생 계산 |
| `src/theme.ts` | 상태→스타일 매핑 | 컴포넌트별 일회성 클래스 |
| `shared/` | 프론트와 워커가 **둘 다** import 하는 도메인 타입·순수 함수 | React, D1, fetch |
| `worker/` | HTTP 라우팅, D1 접근, 외부 어댑터 | JSX, DOM API |
| `migrations/` | 스키마의 **유일한** 정의 | 애플리케이션 로직 |

`src/`는 브라우저 전용이고 `worker/`는 서버 전용이라 공통 타입이 살 곳이 없다.
그래서 `shared/`가 필요하다. `src/types.ts`는 `shared/domain.ts`를 **재수출**만
한다 — 같은 타입을 두 번 정의하면 SSOT 위반이다.

**파일을 쪼개는 것 자체가 목적이 아니다.** 함수 하나짜리 파일을 양산하지 말 것.
경계를 넘는 의존이 생길 때 쪼갠다.

### 4. 트랜잭션 불변식

> 백엔드(D1·Durable Objects) 도입 시점부터 발효한다. 현재는 해당 코드가 없다.

**원자성은 조율이 아니라 제약으로 얻는다.** D1은 단일 라이터라 쓰기 트랜잭션이
교차하지 않고, `db.batch()`는 하나의 직렬화된 트랜잭션이다. "읽고 나서 쓰기"를
**한 문장으로** 표현하면 경합 창 자체가 없다. **Durable Object는 조율에 쓰지
않는다** — 정본을 갖는 순간 두 번째 진실이 생긴다.

| 불변식 | 수단 |
|---|---|
| 수신 멱등 | `mo_key` 부분 유니크 + `ON CONFLICT(mo_key) WHERE mo_key IS NOT NULL DO NOTHING` |
| 발송 멱등 | 클라이언트가 만든 `client_key` 유니크 |
| 동시 편집 | `version` + `UPDATE … WHERE version=?` → `meta.changes`로 판정 |
| 리포트 재생·역순 | `UPDATE … WHERE delivery_status IN (허용 선행 상태)` |
| 읽음 커서 | `ON CONFLICT DO UPDATE SET n = MAX(excluded.n, n)` |
| 담당자 배정 | 조인 테이블에 `INSERT OR IGNORE` / `DELETE` (토글 금지) |
| 정렬키 후퇴 방지 | `WHERE ?new >= last_message_at` 가드 |

- **커밋 우선 (ack가 아니다)** — 웹훅은 **D1 커밋 후에만** `{"code":"10000"}`을
  반환한다. 그 외 코드는 LGU+ 재전송을 부르는데 **그게 설계된 재시도 경로다.**
  먼저 ack하고 `waitUntil`로 미루면 isolate가 죽을 때 고객 메시지를 잃는다.
  응답 전에 미룰 수 있는 건 "무거운 작업"이 아니라 **"내구성과 무관한 작업"**
  (DO 브로드캐스트 등)뿐이다.
- **순서를 믿지 않는다** — 도착 순서가 아니라 `moRecvDt`로 정렬한다. 웹훅
  *호출 간* 순서도 뒤바뀌므로 정렬키 갱신에는 위 가드를 반드시 건다.
- **`INSERT OR IGNORE`로 대체하지 않는다** — NOT NULL·FK·CHECK 위반까지 삼켜서
  고객 메시지를 조용히 버린다. 충돌 타깃을 명시한 `ON CONFLICT`를 쓴다.

---

## 도메인 제약 (LGU+ 메시지허브)

발송·수신을 구현할 때 반드시 지켜야 하는 외부 제약이다.

- **SMS 본문 90 byte / LMS 2,000 byte.** 글자 수가 아니라 바이트로 세고, 길이에
  따라 SMS↔LMS를 자동 전환한다. **바이트는 EUC-KR 기준이라 한글이 2 byte다.**
  `new TextEncoder().encode(s).length`는 **틀렸다** — 그건 UTF-8이라 한글이
  3 byte다. `shared/sms.ts:smsByteLength`가 유일한 구현이고, 코드포인트 단위로
  (`for…of`) 순회한다. `charCodeAt`을 쓰면 서로게이트 쌍이 두 번 세진다.
- **`moRecvDt`는 KST `yyyyMMddHHmmss` 문자열이다.** `new Date(s)`는
  `Invalid Date`를 반환한다. 필드를 직접 파싱하고 +09:00을 빼야 한다.
- **Cron Trigger 최소 주기는 1분이다.** 문서의 "최소 10초 간격" 폴링은 Workers에서
  구현 불가능하다 → 리포트는 웹훅 푸시가 주 경로, Cron은 보정용이다.
- **카카오톡은 사전 승인된 템플릿만 발송 가능하다.** 상담원이 입력한 자유
  텍스트를 알림톡으로 그대로 보낼 수 없다. 자유 텍스트는 SMS/LMS 경로다.
- **요청당 수신자 최대 10명.** TPS도 수신자 수 기준.
- **발신번호는 사전 등록된 것만** `callback`으로 쓸 수 있다.
- **액세스 토큰 1시간.** 리프레시 토큰은 발급 IP에 바인딩되는데 Workers는 고정
  egress IP를 보장하지 않는다 → **refresh를 쓰지 말고 만료 전 재인증한다.**
- **발송 API 200 응답은 *접수*일 뿐이다.** 실제 도달은 리포트로 온다. 메시지
  상태는 `대기 → 접수 → 전송중 → 완료 | 실패`이며, 전이는 허용 선행 상태를
  `WHERE`에 넣어 단조롭게 만든다(재생·역순 리포트가 0행이 되도록).
- **자유 텍스트는 항상 SMS/LMS로 나간다.** 알림톡은 사전 승인 템플릿만 되므로,
  카톡으로 들어온 대화라도 발신은 SMS/LMS다 → **발신 말풍선의 채널 표기가
  `채널톡`이 아니라 `SMS`가 된다.** 원본 디자인과 달라지는 지점이니 그대로 렌더한다.
- **`callback`은 사무소의 기본 발신번호를 서버가 고른다.** 클라이언트가 보낸
  발신번호는 절대 받지 않는다.

---

## 디자인 원본

UI 원본은 Claude Design 프로젝트다. 이 저장소의 `design/prototype.dc.html`이
이식 근거로 남아 있다. **화면 구조나 문구를 바꿀 때는 원본을 먼저 확인한다.**

`.dc.html`은 dc-runtime 포맷이다 — `<x-dc>` 템플릿 + `{{ }}` 바인딩,
`<sc-if>`/`<sc-for>`, 그리고 `renderVals()`가 화면에 넘길 값을 통째로 반환한다.

## 언어

코드 주석·커밋 메시지·문서는 한국어. 식별자는 영문.

**테스트 이름(`describe`/`it`)은 영문으로 쓴다.** miniflare가 테스트 이름을 내부
HTTP 헤더에 실어 보내는데, 비ASCII가 들어가면 실행마다 경고를 뱉는다. 슬라이스가
쌓이면 진짜 실패가 경고 더미에 묻힌다. 테스트 *본문*의 주석은 한국어 그대로 둔다.

도메인 용어(`미처리`/`처리중`/`완료`, `카톡`/`문자`, `대기`/`접수`/`전송중`)는
한글 그대로 타입에 쓰고, **DB 값도 같은 한글을 쓴다** —
`CHECK (status IN ('미처리','처리중','완료'))`. TS 유니온과 D1 CHECK가 글자
그대로 일치하면 매핑 테이블이 필요 없다. 매핑 테이블은 곧 두 번째 진실이다.
